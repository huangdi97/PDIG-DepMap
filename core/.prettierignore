# 不自动格式化的内容：
# - .uvue/.uts（uni-app x 专用语法，工具兼容性未经验证，禁止 formatter 破坏 —— RC_AUDIT PHASE C）
# - fixtures（含 GB18030 字节，禁止归一化）
# - lockfile / 构建产物
*.uvue
*.uts
tests/fixtures/**
node_modules
coverage
dist
package-lock.json
