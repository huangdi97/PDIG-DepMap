# PRODUCT_EXPERIENCE_MAP_V0_2.md

> PDIG / DepMap v0.2.0 产品体验地图：按**用户任务**组织，不按代码模块。
> 每个任务回答 goal §13 八问：①用户为什么来到这个页面 ②用户现在需要知道什么
> ③最重要的信息是什么 ④下一步动作是什么 ⑤用户可能误解什么 ⑥失败时怎么办
> ⑦空状态怎么办 ⑧如何返回。
> Android / Desktop 语义一致（layout 可以不同）；术语以 PRODUCT_TERMINOLOGY_V0_2.md 为准。
> 更新：2026-09-25。

## 任务 1：第一次打开产品

1. **为什么来**：刚安装或刚被推荐，想知道"这是什么、能不能信、要不要用"。
2. **需要知道**：PDIG 帮你整理"你已经知道并确认的数字依赖"，在换卡/到期/注销前告诉你哪些可能受影响。
3. **最重要**：这是本地工具——数据不上传；它不是账本、不是支付 App、不会替你管理账户。
4. **下一步**：新建/打开一个数据文件（Desktop 门页），或直接进入首页（Android 解锁后）。
5. **可能误解**：以为会自动识别全部账户；以为会保存完整账单流水；以为需要联网注册。
6. **失败**：数据文件打不开 → 按错误目录提示（格式/损坏/版本），引导新建；解锁失败 → 重试或换凭据。
7. **空状态**：无数据文件 = 门页（新建/打开）；新数据 = 首页空态 + 明确起点（导入或第一条）。
8. **返回**：任何页面顶部返回；Desktop 左侧导航常驻。

## 任务 2：理解产品做什么

1. **为什么来**：想明白产品边界，决定是否深入。
2. **需要知道**：一句话模型 —— 你确认的依赖 + 变化前的提醒。
3. **最重要**：三个场景（换卡/到期/注销）就是产品入口。
4. **下一步**：进入"常用场景"看三个场景，或去"数据来源"导入第一份数据。
5. **可能误解**：Graph 是首页；它是高级视图。
6. **失败**：无。
7. **空状态**：首页空态不出现 0 Nodes/0 Edges 的工程数字。
8. **返回**：首页。

## 任务 3：导入数据

1. **为什么来**：要让产品有"素材"分析。
2. **需要知道**：支持微信账单、通用 CSV、OFX/QFX、手动添加；文件只在本机解析。
3. **最重要**：隐私声明（不全存流水）与来源说明。
4. **下一步**：选来源 → 选文件 → 预览 → 确认导入。
5. **可能误解**：以为导完就是"发现真实依赖"——实际只产生待确认关系。
6. **失败**：格式不认识/行被跳过 → 明确"哪部分没识别、能不能继续、怎么修"。
7. **空状态**：无来源 →"还没有数据来源，请新建"。
8. **返回**：顶部返回 →"数据来源与导入"。

## 任务 4：理解发现内容

1. **为什么来**：导入后想看看"它发现了什么"。
2. **需要知道**：候选（待确认服务）、待确认关系、可能发生了变化——三者都是"待确认"。
3. **最重要**：机器推断 ≠ 现实。
4. **下一步**：逐条确认/忽略/稍后。
5. **可能误解**：把候选当成"系统已确认"。
6. **失败**：证据缺失 → 显示"为什么出现/来源/依据"，无依据不生成候选。
7. **空状态**："现在没有需要你处理的事项"。
8. **返回**：顶部返回。

## 任务 5：确认服务/关系

1. **为什么来**：把候选与待确认关系变成"我确认的事实"。
2. **需要知道**：确认后会参与影响分析；忽略/拒绝则不再提问（除非有新的充分证据）。
3. **最重要**：A → B、关系类型、能力、依据、为什么建议。
4. **下一步**：确认 / 拒绝 / 稍后。
5. **可能误解**：以为确认=系统验证过；实际=用户断言。
6. **失败**：无。
7. **空状态**：无待确认项。
8. **返回**：顶部返回。

## 任务 6：理解发生了什么变化

1. **为什么来**：看到"可能发生了变化"，想知道之前/现在/依据。
2. **需要知道**：drift 是提醒，不是事实变更。
3. **最重要**：之前确认的信息 ↔ 最近观察到的信息 ↔ 依据。
4. **下一步**：已经换成新的来源 / 两个都在使用 / 没有变化 / 稍后确认。
5. **可能误解**：以为 drift 已修改了数据——它不会。
6. **失败**：无。
7. **空状态**：无变化待确认。
8. **返回**：顶部返回。

## 任务 7：发起一个 Scenario

1. **为什么来**：要换卡/卡到期/注销卡，先做一次影响分析。
2. **需要知道**：三场景分别说明（换卡前检查支付、订阅和钱包依赖 等）。
3. **最重要**：选"哪个支付工具 + 准备做什么 + 什么时候发生"。
4. **下一步**：三选一 → 选择支付工具 → 开始分析。
5. **可能误解**：以为需要填内部参数（不需要）。
6. **失败**：未导入/无确认数据 → 提示先去导入与确认。
7. **空状态**：无支付工具可选 →"先导入并确认一张卡"。
8. **返回**：顶部返回场景中心 → 首页。

## 任务 8：理解 Impact

1. **为什么来**：想知道"换卡会影响谁"。
2. **需要知道**：四类结果 = 必须处理 / 可能还有其他可用方式 / 需要确认 / 当前未发现影响。
3. **最重要**：必须处理项要回答 为什么/依据/依赖哪个已确认关系/不处理会怎样。
4. **下一步**：处理必须事项 → 形成变更计划或继续确认。
5. **可能误解**：把"需要确认"当作错误；把覆盖率当作安全分数。
6. **失败**：分析依赖的确认信息不足 →"需要确认什么"讲清楚。
7. **空状态**：当前未发现影响也明确显示（不是空白）。
8. **返回**：顶部返回。

## 任务 9：形成 ChangePlan

1. **为什么来**：想有条理地完成换卡。
2. **需要知道**：五件事 = 做什么 / 为什么 / 哪些必须做 / 哪些只是建议 / 当前能不能继续。
3. **最重要**：当前状态与 CTA（处理必须事项/继续确认/重新检查/继续下一步/查看验证/查看结果）。
4. **下一步**：按"了解→准备→变更→验证"推进。
5. **可能误解**：把"标记完成"当成"已验证"。
6. **失败**：图谱已变化 → 需要重新检查。
7. **空状态**：无步骤 →"计划里没有需要执行的步骤"。
8. **返回**：顶部返回 → 时间线/首页。

## 任务 10：完成 Action

1. **为什么来**：把计划里的步骤真正做掉。
2. **需要知道**：哪个必须做、哪个建议做。
3. **最重要**：区分「完成操作」与「确认结果」。
4. **下一步**：标记完成 → 需要验证的再确认验证。
5. **可能误解**：Done=verified（错）。
6. **失败**：标记失败 → 保持未完成并可重试。
7. **空状态**：无可执行步骤。
8. **返回**：计划页返回。

## 任务 11：验证是否完成

1. **为什么来**：想知道"换卡真的生效了吗"。
2. **需要知道**：我做完了 ≠ 系统已确认生效。
3. **最重要**：验证四态（待验证/发现新的依据，请确认/已验证/验证失败）。
4. **下一步**：查看新依据并确认；失败则回到步骤。
5. **可能误解**：未来观察=自动验证（最多提示新依据）。
6. **失败**：验证失败 → 明确告诉你回到哪里修正。
7. **空状态**：无需验证。
8. **返回**：计划/时间线。

## 任务 12：回看 Timeline

1. **为什么来**：想复盘"我之前做了什么、接下来有什么"。
2. **需要知道**：时间线是历史视图，删除/隐藏它不会改变现实。
3. **最重要**：事件以人的语言书写（你确认了 …/你完成了…/新的信息可能需要你重新检查）。
4. **下一步**：点开计划/对象继续。
5. **可能误解**：以为时间线=Truth Store（不是）。
6. **失败**：无。
7. **空状态**：还没有需要跟踪的事项。
8. **返回**：顶部返回。

## 任务 13：Backup / Restore

1. **为什么来**：换机/保险/重装前备份，或在新设备恢复。
2. **需要知道**：备份=加密容器；有什么、密码为什么重要、忘记密码会怎样、恢复会发生什么。
3. **最重要**：密码无法找回；恢复会替换当前全部数据。
4. **下一步**：创建备份（设密码→确认→选位置→完成）或恢复（选备份→密码→预检查→摘要→确认→恢复）。
5. **可能误解**：以为备份明文；以为恢复可合并（不可，是替换）。
6. **失败**：密码错/文件损坏/版本过新/格式不支持必须区分提示。
7. **空状态**：未落盘 →"先创建备份"。
8. **返回**：顶部返回。

## 任务 14：删除全部数据

1. **为什么来**：彻底清空这台设备上的记录。
2. **需要知道**：删除范围（数据库、密钥、缓存、工作流状态）+ 不可逆 + 不删除已导出的备份。
3. **最重要**：二次确认 + 不可逆提示。
4. **下一步**：确认删除 → 回到锁定/门页。
5. **可能误解**：以为会删除外部备份文件（不会）。
6. **失败**：部分删除失败 → 明确失败项并允许重试（不留下半状态）。
7. **空状态**：无。
8. **返回**：删除后不返回（回到起点）。

## 附：任务 → 一级入口对照（双端）

| 任务        | Android 入口             | Desktop 入口                 |
| ----------- | ------------------------ | ---------------------------- |
| 1 首开      | 解锁→首页                | 门页（新建/打开）→首页       |
| 3 导入      | 首页→数据来源与导入      | 左侧导航→数据来源→导入       |
| 5 确认      | 首页→需要你处理 / 待确认 | 左侧导航→需要处理→待确认     |
| 7 场景      | 首页→常用场景            | 左侧导航→场景                |
| 9 计划      | 时间线/Impact            | 左侧导航→即将到来 / 影响分析 |
| 12 Timeline | 首页→即将到来            | 左侧导航→即将到来            |
| 13 备份     | 首页→设置→备份/恢复      | 左侧导航→设置→备份与恢复     |
| 14 删数据   | 设置                     | 设置                         |
