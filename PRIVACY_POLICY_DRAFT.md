# PRIVACY_POLICY_DRAFT.md

> 生成时间：2026-09-20（Android Product Finalization）
> 状态：**DRAFT** —— 仅依据真实产品能力撰写；法律主体、联系方式、公开 URL 均为 TODO，不虚构。
> 本草案对应 Play 商店「隐私政策链接」要求（ANDROID_STORE_METADATA.md §10），
> 正式发布前需由用户提供公司主体/联系方式并托管为公开 URL。

---

## 隐私政策（草案）

**生效日期**：TODO（发布前填写）

### 1. 概述

本应用（以下称「本应用」）是一款**本地优先（local-first）**的个人数字基础设施梳理工具，
帮助用户记录账号、支付方式、订阅之间的依赖关系，并在换卡、到期、停用前预览影响面。

**本应用没有账号系统，不收集、不上传任何个人数据。**

### 2. 数据存储

- 用户录入与确认的依赖关系（节点、关系、分组、变更计划）保存在**设备本地**的加密数据库
  （SQLCipher + Android Keystore 包裹的库口令）。
- 应用**不申请网络权限**（Android 清单不含 `INTERNET`），因此技术上无法把任何数据发送到服务器。

### 3. 用户导入的内容

- 用户可主动导入账单文件（微信账单 / 通用 CSV / OFX-QFX）以辅助生成待确认的依赖建议。
- 导入的**原始账单与交易明细只在内存中解析**，用于本次会话内生成 Proposal；
  会话结束即释放，**不会写入数据库，也不会被上传**。
- 数据库内只保留：经过用户确认的依赖关系、来源指纹（用于去重）、导入会话摘要。

### 4. 加密备份

- 用户可主动导出加密备份文件（`.depmap`，AES-256-GCM，口令由用户设置）。
- 备份文件只保存在用户选择的位置（系统文件选择器授权）；本应用不会自动上传或同步。
- **口令无法找回**：丢失口令 = 无法解密备份。

### 5. 删除数据

- 用户可在「设置 → 删除所有数据」删除本机全部记录（数据库、密钥、缓存与工作流状态）。
- 卸载应用同样会删除本地数据。
- 用户自行导出的外部 `.depmap` 文件属于用户文件，本应用不擅自删除。

### 6. 网络与第三方

- 无网络权限、无 analytics、无 telemetry、无广告 SDK、无崩溃上报 SDK。
- 不包含任何第三方统计或跟踪器。

### 7. 权限

- 仅申请 `USE_BIOMETRIC` / `USE_FINGERPRINT`（可选的指纹解锁）。
- 导入/备份/恢复使用系统文件选择器（SAF），由用户显式授权具体文件，不申请全局存储权限。

### 8. 未成年人

本应用不面向未成年人设计，也不在知情情况下收集任何未成年人的个人数据。

### 9. 联系方式

TODO：发布前由用户提供联系方式（邮箱/支持地址）。

### 10. 政策更新

本政策如更新，将在应用商店页面与文档中注明生效日期。

---

## 合规对照

| 商店数据安全表单问题 | 本应用回答 |
|----------------------|-----------|
| 是否收集数据？ | 否（无账号、无遥测） |
| 数据是否加密传输？ | 不适用（无网络传输） |
| 是否可以请求删除数据？ | 是（设置 → 删除所有数据 / 卸载） |
| 是否需要账号？ | 否 |
| 是否含广告/分析？ | 否 |

## TODO 清单（用户提供）

- [ ] 法律主体（公司或自然人运营者）
- [ ] 联系方式（支持邮箱）
- [ ] 隐私政策公开 URL（托管）
- [ ] 生效日期