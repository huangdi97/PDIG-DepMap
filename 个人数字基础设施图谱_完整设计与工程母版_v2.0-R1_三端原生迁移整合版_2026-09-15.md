# 个人数字基础设施图谱 · 完整设计与工程母版

> **英文工作名**：Personal Digital Infrastructure Graph（PDIG）  
> **工程代号**：DepMap  
> **版本**：v2.0-R1 · Native-first Product / Engineering Canonical  
> **日期**：2026-09-15  
> **状态**：**产品语义冻结；MVP01–MVP03 核心已实现并进入三端原生迁移；Native Migration N0 已完成，Android N1/N2 进行中。**  
> **继承关系**：《个人账户关系图》→《个人数字依赖图》v0.4 →《个人数字基础设施图谱》v1.0 → MVP02 / Engineering Baseline / MVP03 / Production Closure → 本 v2.0-R1。  
> **本版定位**：从这一版开始，产品定义、核心语义、MVP01–MVP03 实际实现、UI/UX、工程 Gate、三端原生迁移、发布与后续路线统一到同一份母版中。旧文档保留历史价值，但工程与产品决策以本版为当前 Canonical。

---

## 目录

0. 本版解决什么：从设计母版到“已实现事实 + 原生切换”统一母版
1. 产品定义：个人数字基础设施，而不是账户清单
2. 市场定位与差异：图不是终点，变更闭环才是产品
3. 永久正确性原则与边界
4. 总体产品架构：Evidence → Reality → Impact → Change → Verification
5. Canonical Domain Model：从 Node 到 Living Graph
6. Global Evidence Ingestion：MVP02 后的真实 Source 架构
7. Impact Engine 与 DependencyGroup
8. ChangePlan、PlanReadiness 与 Verification
9. Living Graph：graphRevision / Rebase / Drift / Candidate / Coverage
10. ScenarioTemplate、Timeline 与产品场景层
11. UI / UX：Answer-oriented Native Product
12. 安全、隐私与 `.depmap` 跨端协议
13. Schema、Migration 与数据兼容
14. 三端原生技术架构：Android / iOS / HarmonyOS
15. Native Migration：Legacy Oracle → Canonical Spec → Conformance → Cutover
16. Engineering Baseline、测试与质量门禁
17. 当前真实实现状态（截至 2026-09-15）
18. 已发现并修正的历史语义 / Fixture 问题
19. Revised Roadmap：实际完成顺序与下一阶段
20. 发布、商店、真实数据与最终上线标准
21. Future Backlog：原生基座稳定后再扩
22. 设计决策冻结表  
    附录 A：端到端数据流  
    附录 B：三端原生迁移与 Conformance 流程  
    附录 C：当前 Gate 状态  
    附录 D：术语对照  
    附录 E：新对话接力模板

---

# 0. 本版解决什么

## 0.1 为什么需要 v2.0-R1

v1.0 解决了“这个产品到底是什么、数据模型怎么设计、如何避免错误推断”的问题；随后真正工程落地又发生了三次重要演进：

1. **MVP02** 把“微信账单 parser”提升成真正的全球 Source Abstraction，加入 `SourceInstance`、Generic CSV、OFX/QFX、source-scoped fingerprint、Relation Registry 与 Schema v2；
2. **MVP03** 不再按旧路线直接进入国际支付，而是优先解决“图如何持续变化、计划如何失效、未知如何表达、用户如何从提醒式场景进入变更”的问题，形成 Living Graph & Change Safety；
3. **Production Closure → Native Migration** 证明了 TypeScript / uni-app x 版本的产品与工程语义基本成立，但正式生产路线最终决定彻底切换为 Android / iOS / HarmonyOS 三端原生，并以 Canonical Spec + Conformance 代替“共享同一 Runtime”。

因此旧 v1.0 中“产品定义”大部分仍然成立，但以下内容已经被现实实现更新：

- Schema 已从 v1 演进到 **Logical Schema v3**；
- MVP02 已完成，不再只是 Roadmap；
- MVP03 的实际含义已经变为 **Living Graph & Change Safety**；
- 旧路线中的“International Payment Ecosystem”顺延为 Native RC 之后的 MVP04；
- UI 已从“基础设施 Console”进一步明确为 **Answer-oriented / Scenario-oriented**；
- Production 三端技术栈不再是 uni-app x + UTS，而是三端原生；
- Legacy TypeScript/uni-app x 版本正式冻结为 **Behavior Oracle / Reference Implementation**；
- Canonical Spec、Codegen、Golden Fixtures 与 Cross-platform Conformance 成为新的跨端一致性基础。

## 0.2 本版的最高优先级

产品仍然遵守最初最重要的一句话：

> **宁可漏报，不可把“不确定”伪装成“必须处理”。**

工程层面新增一条同等级原则：

> **跨端共享的不是 Runtime，而是 Canonical Semantics；三端实现必须由同一 Spec、Fixture 与 Conformance Gate 约束。**

## 0.3 本版的真相层级

从 v2.0-R1 起，优先级如下：

1. 本文的冻结原则与 Canonical Spec；
2. Correctness Invariants / Security Invariants；
3. Golden Fixtures / Conformance Expected Results；
4. 已冻结的 Legacy Oracle；
5. 各平台实现；
6. 历史 UI / 旧文档中的未冻结描述。

如果 Legacy 行为与 Canonical Spec 冲突，不能因为“旧代码已经这么跑”就把旧 bug 永久复制到 Kotlin / Swift / ArkTS。

## 0.4 外部市场信息说明

本版保留 v1.0 的竞品和行业定位框架，但**本轮没有重新联网核验所有竞品、价格、功能或市场状态**。市场部分用于说明产品边界和差异，不作为 2026-09-15 的实时商业情报报告。

---

# 1. 产品定义：个人数字基础设施，而不是账户清单

## 1.1 一句话

> **换卡、换号、换邮箱、换设备、关账户之前，先知道什么会受影响。**

英文工作表达：

> **Know what breaks before you change it.**

更完整的产品定义是：

> **一个人的数字生活由哪些身份、账户、资金、设备、服务和数字资源支撑；这些对象之间如何形成依赖；当其中一个对象被更换、失去、注销、过期或故障时，会影响什么，以及应该按什么顺序完成变更、核对和验证。**

## 1.2 产品不是“清单”，而是一套私人基础设施

真实用户可能拥有：

- 身份：主手机号、备用手机号、主邮箱、工作邮箱、身份证件、身份提供方；
- 资金：国内银行账户、Checking、Savings、Wise、Revolut 等；
- 支付工具：Credit / Debit Card、Virtual Card；
- 支付平台：微信、支付宝、PayPal、Apple / Google / Harmony 相关支付账户；
- 核心账户：Apple Account、Google、Microsoft、GitHub；
- 服务：Netflix、Spotify、iCloud、ChatGPT、AWS、Cloudflare；
- 设备：手机、笔记本、安全密钥、U 盾；
- 数字资源：域名、Repo、服务器、云文档、API Project。

产品关心的不是“你有 37 个账户”，而是：

```text
银行卡 / 银行账户
      ↓
支付平台 / 钱包
      ↓
服务 / 订阅
      ↓
当资金来源变化时，哪些关系必须处理，哪些只是建议确认？
```

以及长期：

```text
手机号 / 邮箱
      ↓
账户访问与恢复
      ↓
被这些账户控制的服务、设备和数字资源
```

## 1.3 四层核心价值

### Layer 1 — Inventory

我有什么？

身份、账户、资金工具、设备、服务、数字资源。

### Layer 2 — Dependency

什么依赖什么？

付款、访问、恢复、身份、控制、托管、数据、通信等依赖。

### Layer 3 — Impact

如果 X 的某个 capability 不可用，会怎样？

使用 capability-aware counterfactual simulation，输出：

- `must_change`；
- `backup_path` / redundancy degraded；
- `degraded`；
- `needs_review`；
- `unaffected`。

### Layer 4 — Change

我要改变 X，怎么安全完成？

```text
Scenario
→ Impact
→ ChangePlan
→ Actions
→ Change
→ Verification
```

**Graph 不是产品终点；ChangePlan 才是用户价值闭环。**

## 1.4 产品不替代什么

PDIG 不替代：

- 密码管理器；
- 系统钱包 / NFC 支付；
- 银行 App；
- 记账软件；
- 投资 / 财富管理软件；
- 通用提醒 App；
- 数字遗产法律工具；
- 自动代用户登录并修改支付方式的机器人。

密码、完整卡号、CVV、助记词、恢复码、私钥仍应由密码管理器 / 安全硬件管理。PDIG 只记录必要的语义关系和 `vaultRef` / `walletRef` 等指针。

## 1.5 什么才值得成为基础设施节点

“经常使用”不等于“基础设施”。一个网站 / App / 服务至少满足以下之一，才值得进入核心图：

- 承载身份或账户；
- 是付款 / 收款 / 自动续费链路的一部分；
- 承担访问、2FA、恢复、身份验证；
- 控制或托管重要数字资源；
- 保存影响个人或工作连续性的数据；
- 其失效会导致其他对象不可用或需要变更。

---

# 2. 市场定位与差异

## 2.1 相邻类别

原 v1.0 已识别出几类相邻产品：账户地图、数字依赖/恢复图、订阅管理、密码管理器、Digital Legacy 等。它们证明“账户关系”“blast radius”“恢复路径”“支付方式管理”分别存在需求。

PDIG 的差异不应表述成“别人没有图”，而应表述为：

> **Evidence-backed discovery → Human-confirmed reality → Capability-aware impact → ChangePlan → Verification。**

即：发现、确认、反事实模拟、变更计划和变更后验证形成闭环。

## 2.2 产品护城河不在 Parser

账单解析、CSV 映射、OFX/QFX 解析本身不是长期护城河。真正难复制的是：

- Reality 边界；
- 不确定性处理；
- DependencyGroup 与 capability 语义；
- 可解释 Impact；
- Living Graph；
- ChangePlan 与 Verification；
- local-first 安全协议；
- 多来源证据与 Source Contract；
- 跨端一致性 Conformance。

## 2.3 不把“支付网络自动更新”误判为安全

海外 card updater、wallet fallback、mandate 等只能成为 continuity hint，不能自动让系统输出“无需处理”。

只有：

- 用户明确确认；或
- 未来真正 authoritative source 明确证明；

才允许转成 Confirmed Reality 或 Confirmed Group。

---

# 3. 永久正确性原则与边界

以下原则从 v0.4 延续至 v2.0-R1，并已成为实现与测试的根本约束。

## 3.1 Reality Boundary

- `Observation != Dependency`
- `Proposal != Reality`
- `DiscoveryCandidate != Node`
- `RealityDrift != Reality mutation`
- `Evidence != Transaction History`
- `Recorded absence != real-world absence`
- `Reachability != failure propagation`
- `Node failure != all capabilities failure`
- `Two edges != confirmed fallback`
- `done != verified`
- `ScenarioCoverage != PlanReadiness`
- `TimelineItem != Truth`
- `ScenarioTemplate != Reality`
- `confidence != confirmation`
- Future observation != automatic verification

## 3.2 Precision over Recall

所有高后果输出默认偏向 precision：

- 少报比错报“必须处理”好；
- fuzzy match 不能直接成为 Node identity；
- machine inference 不能直接创建 `required` Dependency；
- 未确认 Group 不能作为自动备用路径；
- 缺数据不等于现实不存在。

## 3.3 Capability 是传播单位

Impact 传播状态不是 node-level，而是：

```text
ImpactStateKey = (nodeId, capability)
```

当前生产域仍然主要是：

```text
payment
```

`access / recovery / identity / control / hosting / data / communication` 属于长期词表，不得因为 Native Migration 顺手提前扩大业务范围。

## 3.4 Graph 是 state model，不是主界面

用户不需要理解：

- Node count；
- Edge count；
- Graph Revision；
- Relation Registry；
- SourceInstance ID。

用户需要理解：

- 现在有没有事情需要处理；
- 为什么；
- 接下来做什么；
- 哪些是必须、哪些只是建议确认；
- 当前结论基于什么；
- 还有哪些未知。

---

# 4. 总体产品架构

## 4.1 端到端主链

```text
Evidence Source
    ↓
Observation（事实，会话内）
    ↓
Normalization / Fingerprint / CanonicalEvent
    ↓
Node Resolution
    ↓
DependencyProposal / GroupProposal
    ↓
Human Confirmation
    ↓
Confirmed Reality Graph
    ↓
Impact Engine
    ↓
Scenario / ChangePlan
    ↓
Actions
    ↓
Verification
    ↓
Living Graph / Rebase / Drift / Timeline
```

## 4.2 五个用户层能力

1. **建立**：通过导入、手工录入和未来 discovery 建立基础设施；
2. **确认**：把机器发现与现实确认分开；
3. **理解**：解释依赖、覆盖与未知；
4. **变更**：以 Scenario / ChangePlan 驱动换卡、注销、到期等动作；
5. **维持**：通过 Drift、Freshness、Timeline、Verification 保持图与现实接近。

---

# 5. Canonical Domain Model

## 5.1 Stable Node Kinds

长期 coarse kinds：

| kind                 | 含义               | 示例                               |
| -------------------- | ------------------ | ---------------------------------- |
| `identity_anchor`    | 身份与联系锚点     | 手机号、邮箱、身份证件             |
| `financial_account`  | 存放 / 结算资金    | Checking、Savings、银行账户        |
| `payment_instrument` | 支付工具           | Credit / Debit / Virtual Card      |
| `account`            | 某服务中的具体账户 | PayPal Account、Apple Account      |
| `service`            | 服务/平台          | GitHub、Netflix、AWS               |
| `device`             | 设备               | 手机、电脑、安全密钥               |
| `digital_resource`   | 重要数字资产       | 域名、Repo、Server、Cloud Document |
| `membership`         | 会员关系           | 航空、酒店、健身                   |
| `custom`             | 自定义对象         | 未标准化实体                       |

国家、地区、币种、支付网络都属于 metadata，不把国家写进 kind。

## 5.2 `financial_account` 与 `payment_instrument` 永久分离

关闭 Checking Account 与更换其 Debit Card 是完全不同的事件，blast radius 也不同。因此：

```text
financial_account
   ↓ funding / issuance / future relation
payment_instrument
```

不能继续用“银行卡”一个节点承载所有银行账户语义。

## 5.3 `service` / `account` / `digital_resource` 边界

```text
GitHub（service）
  └─ 我的 GitHub Account（account）
       └─ repo/foo（digital_resource）
```

## 5.4 SourceInstance（MVP02 已实现）

MVP02 后，数据源不再只由 parser 名称表示，而是由 `SourceInstance` 表示一次可区分的现实来源实例。

关键字段：

```text
id
adapterId
adapterVersion
sourceKind
providerId?
accountNodeId?
label
country/jurisdiction/currencies?
state
createdAt / updatedAt
lastIngestedAt
```

这解决了：同一个 adapter 处理两个不同银行账户 / 文件来源时，fingerprint 和 evidence 不能互相污染。

## 5.5 Source Contract

```text
SourceKind:
statement_file
platform_export
open_banking
manual
discovery

CoverageMode:
event_stream
partial_snapshot
complete_snapshot
user_selected
```

当前文件 adapter 均按 `event_stream` 对待，`authoritativeFor=[]`。**事件流缺失记录永远不能自动 retire Confirmed Reality。**

## 5.6 Observation

Observation 只表达“发生过什么”，默认会话内存活，不长期保存完整交易流水。

## 5.7 CanonicalEvent

用于会话内把多个来源中疑似同一经济事件关联起来，不物理合并，不不可逆去重。

## 5.8 Fingerprint v2（MVP02）

MVP02 后唯一范围升级为：

```text
UNIQUE(sourceInstanceId, fingerprintVersion, fingerprint)
```

稳定交易号的 HMAC scope 也包含 SourceInstance：

```text
adapterId + ":" + sourceInstanceId + ":" + sourceTxnId
```

`fpSecret` 保存在加密数据库中，并随 `.depmap` 迁移。

## 5.9 EvidenceSummary

Evidence 仍然是聚合摘要，不是交易史。MVP02 后包括：

- `sourceInstanceId`；
- adapter / version；
- evidenceKind；
- first / last observed；
- observation count；
- provenance。

跨来源的 counts 不盲目相加，multi-source 首先增加 provenance，不自动增加“真实性 certainty”。

## 5.10 DependencyProposal

Proposal 是机器推断，不进 Confirmed Reality Graph。

每个 suggested dependency 自己拥有 decision：

```text
pending
accepted
rejected
```

Proposal.status 仅用于 UI / filter；业务逻辑读 suggestion decisions。

被拒 Proposal 只有在有真正新 evidence 时才允许重提。当前规则：

- 拒绝后新增 Observation >= 3；
- 且跨至少一个完整识别 / recurring period；
- MVP02 多来源阈值必须由某一独立 evidence stream 自己满足，不能简单跨源求和。

## 5.11 Dependency

只有用户确认或未来权威来源确认后才存在。

核心字段：

```text
id / from / to / relation / capability
criticality = required | unknown
state = active | retired
origin = manual | proposal
confirmedAt / lastVerifiedAt / retiredAt
evidenceRefs[]
```

逻辑唯一键：

```text
(from, relation, to, capability)
```

reactivation 必须复用原 ID。

## 5.12 DependencyGroup

Group 是现实断言，不是机器统计结果。

```text
mode = ANY | ALL
targetNodeId
capability
memberEdgeIds
groupKey
state
timestamps
```

Group 必须显式确认。多条 funding source 不等于已经存在 fallback。

`groupKey` 对成员排序 canonicalize，避免 `[A,B]` 与 `[B,A]` 产生两组。

## 5.13 RelationDefinitionRegistry（MVP02）

当前 production relation：

```text
funding_source
merchant_agreement
```

Registry 负责验证：

- from/to kinds；
- capability；
- Group policy；
- default criticality policy；
- verification policy。

Future 关系：

```text
card_on_file
direct_debit_mandate
payout_destination
wallet_funding
verifies
recovers
authenticates
controls
hosts
resolves_dns_for
deploys_to
stores_data_for
```

本轮 Native Migration 不启用 Future relation。

## 5.14 graphRevision（MVP03）

`graphRevision` 是 Confirmed Reality Graph 的语义版本。

只有以下 Reality mutation bump：

- Dependency create/confirm/retire/reactivate；
- confirmed criticality change；
- Group confirm/retire；
- confirmed group membership change。

以下不 bump：

- Observation；
- Evidence；
- Fingerprint；
- Proposal；
- Candidate；
- Drift creation；
- Timeline projection；
- Verification suggestion。

Reality mutation 与 revision +1 必须同事务。

---

# 6. Global Evidence Ingestion：MVP02 后的真实 Source 架构

## 6.1 EvidenceSourceAdapter

长期抽象：

```text
EvidenceSourceAdapter
  ├─ canHandle
  ├─ parse
  ├─ normalize
  └─ source contract metadata
```

Parser 只是 statement_file adapter 的一个实现，不是架构中心。

## 6.2 当前已经实现 / 验证的 Source

### WeChat Statement

最早的 payment vertical slice，证明：

```text
payment_instrument → WeChat account → service
```

可以由真实 evidence 发现并通过人工确认转成 Reality。

### Generic CSV Mapper（MVP02）

- 明确字段映射；
- 不使用 LLM 猜字段；
- 支持 US / EU 常见字段结构；
- 支持 debit / credit；
- BOM；
- quoted comma；
- CRLF / CR-only；
- malformed date / amount；
- multicurrency；
- 10k 行压力测试。

### OFX / QFX（MVP02）

核心字段：

```text
FITID
DTPOSTED
TRNAMT
TRNTYPE
NAME
MEMO
```

优先使用 FITID 作为稳定来源 ID，缺失时使用 fallback fingerprint。

## 6.3 Discovery 仍然只能产生 Candidate

未来浏览器、书签、App、平台导出等 discovery source：

```text
Observation / metadata
→ DiscoveryCandidate
→ Human Confirmation
→ Node
```

不能：

```text
高频访问
→ 自动 Node
```

更不能保存完整 URL、搜索词、页面内容和完整浏览历史。

---

# 7. Impact Engine 与 DependencyGroup

## 7.1 API

Canonical API：

```text
simulateScenario(unavailable: Set<ImpactStateKey>)
```

便捷包装：

```text
simulateDisable(nodeId, capability='payment')
```

## 7.2 状态键

```text
ImpactStateKey = (nodeId, capability)
```

不能再使用对象 identity 作为 Set 去重语义；工程 Baseline 的 property test 已经实际发现并修复过类似 value-dedup 问题。

## 7.3 规则顺序

1. 有 active confirmed Group：按 Group 判断；
2. 同目标有其他 active Dependency 但没有 confirmed Group：`needs_review`；
3. required edge 且没有确认替代路径：capability lost；
4. unknown：`needs_review`；
5. Proposal / unconfirmed group：最多 `needs_review`。

## 7.4 输出

```text
must_change
backup_path
/degraded
needs_review
unaffected
```

其中 `must_change` 只能来自 Confirmed Reality 的高确定性失效。

## 7.5 防环与确定性

使用 BFS / queue + value-level visited；输出顺序必须 deterministic，供三端 Conformance 直接比较 normalized result。

---

# 8. ChangePlan、PlanReadiness 与 Verification

## 8.1 ChangePlan 是产品核心

不是“分析报告”，而是一次现实变更的执行记录。

长期流程：

```text
Understand
→ Prepare
→ Change
→ Verify
```

## 8.2 Rebase（MVP03）

ChangePlan 保存：

```text
baselineGraphRevision
lastAnalyzedGraphRevision
```

若当前 graphRevision 已变化，非终态 Plan 派生：

```text
needs_revalidation
```

重新 simulate 后生成 deterministic diff：

- added impacts；
- removed impacts；
- changed impacts；
- added actions；
- removed actions；
- changed actions。

completed / cancelled 的历史计划不得被自动重新打开。

## 8.3 PlanReadiness

只允许三态：

```text
blocked
review_required
ready_with_known_scope
```

禁止“safe”“100% 安全”“完全没问题”。

### blocked

存在未解决 `must_change` requirement。

### review_required

存在：

- `needs_review`；
- unknown；
- unresolved candidate；
- relevant pending proposal；
- stale dependency；
- revision mismatch；
- required action 尚未验证等。

### ready_with_known_scope

只有当前已知、已确认范围中的 required requirements 被真正 resolution mapping 解决，并且 graph revision 当前有效时才能出现。

## 8.4 Readiness 的 P0 铁律

绝不能用：

```text
mustChangeTargetCount - completedActionCount
```

判断 Ready。

必须显式映射：

```text
ActionItem.resolvesImpactKeys[]
```

或等价 requirement mapping。

因为：

- 一个 target 可能需要多个 required actions；
- 多个 targets 可能共享一个 action；
- unrelated completed action 不能抵消 must_change。

## 8.5 Verification

`done != verified`。

Verification strategies：

```text
manual_confirmation
future_observation
authoritative_source（future）
```

状态：

```text
not_required
pending
evidence_suggested
verified
failed
```

future observation 只能推进到 `evidence_suggested`；用户确认后才 `verified`。

---

# 9. Living Graph：MVP03 实际实现

## 9.1 为什么 MVP03 没有直接进入 International Payment

真实工程发现：如果图不能表达“现实已经变化”“计划分析已经过期”“未知是否充分”“候选是不是现实”，继续增加国际 payment relation 只会把错误传播范围放大。

因此实际 MVP03 先实现 **Living Graph & Change Safety**。

## 9.2 RealityDrift

Drift 表达：

> 新的正向 evidence 与当前 Confirmed Reality 不一致，或暗示现实可能已经改变。

只允许 positive evidence 触发；absence-only 永远不能触发 drift。

用户可选择：

- 已经换成新来源；
- 两个都在使用；
- 没有变化；
- 稍后确认。

Drift 自身不修改 Graph。只有用户明确确认 replacement / additional path 后，才进入 Reality transaction，并 bump graphRevision。

## 9.3 DiscoveryCandidate

Candidate 与 Proposal 是两条不同链：

```text
Observation → Candidate → User → Node
Observation → Proposal → User → Dependency
```

Candidate：

- 不进入 Impact；
- 不 bump revision；
- accept 创建一个 logical Node；
- dismiss 后只有真正新的 evidence 才允许保守重现。

## 9.4 ScenarioCoverage

Coverage 不再使用假的 0–100 分数。

状态：

```text
unknown
limited
partial
well_evidenced
```

解释来源包括：

- source freshness；
- confirmed dependencies；
- pending proposals；
- unresolved candidates；
- stale dependencies；
- unknown criticality；
- verification gaps。

Coverage 回答“我们知道多少”，Readiness 回答“基于当前已知信息，现在能不能继续”，两者永远不能混为一谈。

---

# 10. ScenarioTemplate、Timeline 与产品场景层

## 10.1 为什么引入 ScenarioTemplate

用户通常不是为了“维护一张图”打开 App，而是因为一个现实事件：

- 我要换卡；
- 卡快到期；
- 我要注销某个支付工具。

因此场景模板是产品入口，但不能退化成万能提醒 App。

## 10.2 当前 Active 场景

当前只启用 payment-domain 已有能力真正支持的三个模板：

```text
replace_payment_card
expiring_payment_card
close_payment_instrument
```

其他模板如果没有完整 Domain factory，只能 `planned / factory=null`，不得出现可点击的假功能。

## 10.3 非 PDIG 场景禁止混入

生日、喝水、植物、考试、普通会议、节日、健身等不属于数字基础设施连续性，不进入 PDIG Scenario Catalog。

## 10.4 Timeline

Timeline 是 projection / read model，不是第二真相源。

来源可以包括：

- ChangePlan effective date；
- Node expiry；
- Action due；
- Verification due；
- Drift pending；
- freshness review。

排序：

```text
overdue / attention
→ today
→ 7d
→ 30d
→ 90d
→ later
```

删除 / 隐藏 TimelineItem 不得修改 Reality Graph。

---

# 11. UI / UX：Answer-oriented Native Product

## 11.1 总原则

旧 v1.0 已明确“Graph 不是首页”；后续 UI 讨论进一步冻结为：

> **首屏先回答用户问题，而不是展示系统内部数据结构。**

## 11.2 首页信息架构

首页优先：

1. **需要你处理**；
2. **可能发生变化**；
3. **即将到来**；
4. **常用场景**；
5. **我的基础设施**。

不把：

```text
Nodes 42
Dependencies 83
Sources 5
Schema v3
Graph Revision 17
```

作为主视觉。

## 11.3 “需要你处理”聚合

包括：

- pending Proposal；
- RealityDrift；
- unresolved Candidate；
- `needs_revalidation` Plan；
- pending Verification；
- blocked Plan；
- review_required Plan。

## 11.4 场景中心

当前展示：

- 更换银行卡；
- 银行卡即将到期；
- 注销银行卡。

建议文案：

**更换银行卡**  
换卡前检查支付、订阅和钱包依赖。

**银行卡即将到期**  
到期前看看还有哪些支付关系依赖这张卡。

**注销银行卡**  
注销前确认哪些支付关系需要迁移。

## 11.5 ChangePlan Detail

页面至少回答：

- 我要做什么？
- 为什么？
- 哪些必须做？
- 哪些只是建议确认？
- 我现在能不能继续？
- 当前判断基于哪些数据？
- 还有哪些未知？

CTA：

```text
blocked              → 处理必须事项
review_required      → 继续确认
needs_revalidation   → 重新检查
ready_with_known_scope → 继续下一步
verifying            → 查看验证
completed            → 查看结果
```

## 11.6 Coverage UI

不显示“87 分”“94% 安全”。

显示：

- 本次分析依据；
- 数据来源；
- 最近更新时间；
- 已确认关系；
- 待确认；
- 未解析；
- 长期未验证。

## 11.7 Drift UI

用户看到：

```text
可能发生了变化

当前已确认：……
最近观察到：……

[已经换成新来源]
[两个都在使用]
[没有变化]
[稍后确认]
```

不能表现成系统已经自动修改现实。

## 11.8 Candidate UI

表达：

> 发现一个可能属于你的服务

用户可以：确认、忽略、稍后。

## 11.9 Import UX

统一流程：

```text
选择来源
→ 隐私说明
→ 选择文件
→ 解析预览
→ CSV 字段映射（如需要）
→ Node Resolution
→ Proposal Review
→ 完成
```

导入前必须明确：

- 文件本地处理；
- 原始流水不作为历史账本长期保存；
- 只保留必要的 fingerprint、evidence summary 与用户确认结果。

## 11.10 Onboarding

控制在 3 屏以内：

1. 看清你的数字依赖；
2. 换卡前先知道哪些服务会受影响；
3. 数据默认在本地处理。

## 11.11 原生 UI 决策

三端不追求像素级相同，但必须：

- 品牌一致；
- 信息架构一致；
- 关键 CTA 一致；
- Domain semantics 一致；
- 使用各平台自然交互。

Android：Compose / Material 3；  
iOS：SwiftUI；  
HarmonyOS：ArkUI。

Design Tokens 与关键中文 copy 进入 `spec/ui`，避免三端文案和语义漂移。

---

# 12. 安全、隐私与 `.depmap`

## 12.1 威胁模型

PDIG 图本身就是敏感数据，它可能暴露：

- 主手机号 / 邮箱；
- 银行关系；
- 支付路径；
- 订阅；
- 恢复结构；
- 潜在单点依赖。

因此 local-first 不是“营销偏好”，而是架构要求。

## 12.2 本地持久化

### Android

```text
SQLCipher
+ Android Keystore
+ BiometricPrompt / Device Credential
```

### iOS

```text
SQLCipher
+ Keychain
+ LocalAuthentication
```

### HarmonyOS

```text
ArkData relationalStore encryption
+ HUKS
+ Harmony 用户认证能力
```

三端逻辑 Schema 一致，物理数据库实现无需一字不差。

## 12.3 原始交易数据红线

- Raw statement 仅导入会话处理；
- Observation / CanonicalEvent 默认 memory-only；
- 不建立长期交易账本；
- 不保存完整交易时间线；
- Evidence 只保留聚合摘要；
- raw file buffer、parsed rows、Observation arrays 在导入结束后释放引用。

## 12.4 `.depmap` 协议冻结

```text
DEPMAP_CONTAINER_V1
```

保持不变：

```text
format = depmap
formatVersion = 1
Argon2id version = 19
salt = 16 bytes
memoryKiB = 65536
iterations = 3
parallelism = 1
key = 32 bytes
AES-256-GCM
nonce = 12 bytes
tag = 16 bytes
Base64 = RFC4648 padded
password = exact UTF-8 bytes
no Unicode normalization
AAD = RFC8785-JCS({format, formatVersion, kdf, cipher})
```

生产导出参数固定 65536 / 3 / 1；Header 在 KDF 前必须进行 hostile-input bounds validation。

## 12.5 Golden Vector

Native Migration 中 `.depmap` Golden Vector 已升级成跨端 Conformance 核心。

当前 Android Kotlin 已做到：

- derivedKey 与 frozen Node/TS reference 逐字节一致；
- ciphertext 逐字节一致；
- tag 逐字节一致；
- JCS 规范向量通过。

这类逐字节一致性比“能解密”更强，是切换三端原生的关键证据。

## 12.6 不自研密码学 primitive

允许写 protocol glue，不允许自行实现 Argon2 / AES-GCM / CSPRNG。

每端必须记录 crypto dependency、版本、license 与维护状态。

---

# 13. Schema、Migration 与数据兼容

## 13.1 实际 Schema 演进

### Schema v1

MVP01 初始 Reality / Proposal / Evidence / Group / Impact 结构。

### Schema v2（MVP02）

核心变化：

- `SourceInstance`；
- source-scoped fingerprint；
- multi-source evidence provenance；
- RelationDefinitionRegistry；
- Generic CSV / OFX / QFX；
- legacy source migration。

### Schema v3（MVP03）

加入 Living Graph：

- graphRevision；
- ChangePlan rebase metadata；
- RealityDrift；
- DiscoveryCandidate；
- Verification / Timeline 相关持久化；
- Scenario / Living Graph supporting tables。

## 13.2 Container 与 Payload 分层

Native Migration 期间：

- `.depmap` Container 继续 `formatVersion = 1`；
- decrypted payload `schemaVersion = 3`；
- v1 / v2 payload 必须可以迁移到 v3；
- future schema 必须明确 reject；
- 不允许把 Native 重写当成“Schema 重新从 1 开始”。

## 13.3 Migration 原则

- transactional；
- failure rollback；
- IDs preserve；
- decisions preserve；
- Evidence preserve；
- Group preserve；
- SourceInstance preserve；
- revision 语义正确；
- restart-safe；
- 迁移失败不自动清库。

## 13.4 Native Cutover 用户迁移

旧版如果没有公开用户，可主要通过 `.depmap` 完成迁移；如果已经发布并存在用户，还需评估是否保持相同 applicationId / bundle identifiers 及 in-place DB upgrade。

当前母版要求 Native App 至少能够读取现有 `.depmap`，这是 Legacy Cutover 的硬 Gate。

---

# 14. 三端原生技术架构

## 14.1 最终决策

Production 正式退出：

- uni-app；
- uni-app x；
- UTS runtime；
- HBuilderX build pipeline；
- DCloud Cloud Build；
- DCloud Native SDK；
- DCloud login / pack 作为 Production 必需条件。

Legacy 版本保留为 Reference，不再作为未来 Production Runtime。

## 14.2 Android

```text
Kotlin
Jetpack Compose
Material 3
Navigation Compose
Coroutines / Flow
SQLite / SQLCipher
Android Keystore
BiometricPrompt
Gradle
```

推荐层：

```text
presentation
application
domain
data
platform
security
```

Domain 禁止依赖 Compose / Context / SQLite / Keystore。

## 14.3 iOS

```text
Swift
SwiftUI
NavigationStack
Observation / async-await
SQLite / SQLCipher
Keychain
LocalAuthentication
Xcode / Swift Package
```

Domain 禁止依赖 SwiftUI / UIKit / Keychain implementation / SQLite implementation。

## 14.4 HarmonyOS

```text
ArkTS
ArkUI
Stage Model
ArkData relationalStore
HUKS
Harmony 用户认证
DevEco / hvigor
```

Domain 禁止依赖 ArkUI / Ability / ArkData implementation。

## 14.5 为什么最终选择三端原生

选择 C 不是因为跨端框架一定不行，而是因为本产品具有以下特征：

- local-first；
- 敏感本地数据；
- 加密数据库；
- 平台原生密钥系统；
- 生物认证；
- 文件导入 / 导出；
- backup / restore；
- 生命周期与后台隐私；
- 商店和 signing；
- 三端都要求正式上架。

AI 辅助降低三端重复实现成本，但不能代替 Conformance。最终采用：

> **一个 Canonical Spec + 三个原生实现 + 一套跨端 Conformance。**

---

# 15. Native Migration

## 15.1 Legacy 的角色

Legacy TypeScript / uni-app x 已冻结为：

- Reference Implementation；
- Behavior Oracle；
- Fixture Generator；
- Migration Source；
- Conformance Reference。

当前冻结 tag：

```text
v0.3.0-uniapp-reference
```

## 15.2 Canonical Spec

`spec/` 已经机器可读，覆盖：

- 枚举；
- 实体；
- relation；
- scenario；
- state machines；
- error codes；
- logical schema v3；
- security policy；
- design tokens；
- Chinese copy。

## 15.3 Codegen

从 Canonical Spec 生成：

```text
Kotlin
Swift
ArkTS
```

静态定义，例如 RelationType、Capability、PlanReadiness、ScenarioTemplateId、ErrorCode 等，不允许三端手工维护不同版本。

Generated code 由 Gate 检查；手改 generated file 必须失败。

## 15.4 Golden Fixtures

当前 N0 已建立：

- 64 个平台中立用例；
- 28 个三端共享原始输入 fixture；
- manifest + sha256；
- Legacy Oracle 逐字节自检。

当前分类覆盖包括：

```text
impact
readiness
coverage
relations
depmap
jcs
scenario
migration
state-machine
```

后续仍需把 Parser / Backup / richer Migration / Timeline 提升为一等跨端 Conformance 类别。

## 15.5 Conformance Harness

正确的总状态口径应拆成：

```text
CONFORMANCE_HARNESS = PASS
LEGACY_ORACLE_SELFCHECK = PASS
ANDROID_CONFORMANCE = PASS
HARMONY_CONFORMANCE = NOT_RUN（当前）
IOS_CONFORMANCE = NOT_RUN（当前）
CROSS_PLATFORM_CONFORMANCE = PARTIAL_WITH_REPORT（当前）
```

不能因为 Harness 本身全绿，就把“三端跨平台 Conformance”写成 PASS。

## 15.6 Differential Testing

迁移期间使用：

```text
same fixture
    ↓
Frozen TS Oracle
Kotlin
Swift
ArkTS
    ↓
Normalized semantic diff
```

忽略随机 UUID、运行时间戳、平台路径，只比较业务语义。

## 15.7 Migration Phase

```text
N0  Canonical Specification / Legacy Freeze / Codegen / Fixtures / Harness
N1  Android Vertical Slice
N2  Android Full Parity
N3  HarmonyOS Full Parity
N4  iOS Full Parity / Source Parity
N5  Cross-platform Conformance
N6  Legacy Cutover
N7  Native Production RC
```

## 15.8 Cutover 条件

Production 才能真正移除 DCloud / UTS / uni-app，当且仅当：

- Android Native parity PASS；
- Harmony Native parity PASS；
- iOS Native Source parity PASS，且有 Mac 时完成 Build；
- Core Conformance PASS；
- Crypto Golden PASS；
- Migration PASS；
- Backup / Restore PASS；
- UI critical flow parity PASS；
- Security PASS；
- `.depmap` compatibility PASS。

---

# 16. Engineering Baseline、测试与质量门禁

## 16.1 已建立的工程基线

Legacy 工程已建立并反复验证：

- format；
- lint；
- strict typecheck；
- architecture boundary；
- circular dependency；
- unit / integration / contract / migration / regression；
- invariant；
- property-based；
- fuzz；
- mutation targeted；
- performance；
- security/privacy；
- dependency / license；
- clean install / clean clone；
- secret / network / logging scans。

当前 Frozen Legacy Oracle 实跑：

```text
43 files / 453 tests PASS
```

## 16.2 Native Engineering Baseline

Native 版本必须继承同样的纪律，而不是因为换语言就退回“能编过就算完成”。

每个平台至少要求：

### Android

- Kotlin compile；
- lint / format；
- domain unit；
- repository integration；
- migration；
- crypto golden；
- conformance；
- Compose UI state / UI test；
- APK/AAB build；
- emulator/device E2E。

### HarmonyOS

- ArkTS compile；
- domain unit；
- ArkData migration；
- HUKS/security；
- conformance；
- ArkUI state / runtime；
- hvigor build；
- emulator/device smoke。

### iOS

- Swift compile；
- domain unit；
- SQLCipher repository；
- migration；
- crypto golden；
- conformance；
- SwiftUI state / UI test；
- Simulator / Device / Archive。

## 16.3 Native Definition of Done

一个功能不是“某端代码写完”就完成，而是：

```text
Spec updated
→ Fixture updated
→ Platform implementation
→ Unit / Integration
→ Conformance
→ UI state
→ Runtime（可用平台）
→ Docs / Matrix updated
```

## 16.4 真实状态枚举

所有 Gate 统一使用：

```text
PASS
FAIL
BLOCKED
NOT_RUN
PARTIAL_WITH_REPORT
```

禁止“基本完成”“理论可运行”“应该可以”。

---

# 17. 当前真实实现状态（截至 2026-09-15）

## 17.1 Native Migration Phase

| Phase                         | 状态                | 当前事实                                                |
| ----------------------------- | ------------------- | ------------------------------------------------------- |
| N0-A 仓库恢复                 | PASS                | Git audit 完成                                          |
| N0-B Legacy Freeze            | PASS                | `v0.3.0-uniapp-reference`                               |
| N0-C Canonical Spec           | PASS                | `spec/` 机器可读                                        |
| N0-D Codegen + Gate           | PASS                | Kotlin/Swift/ArkTS generated                            |
| N0-E Golden Fixtures          | PASS                | 64 cases + 28 raw fixtures + hashes                     |
| N0-F Conformance Harness      | PASS                | Harness 全绿                                            |
| N1 Android Vertical Slice     | PARTIAL_WITH_REPORT | Domain/Crypto/Conformance PASS；data/security/UI 未完成 |
| N2 Android Full Parity        | NOT_STARTED         | 正在进入                                                |
| N3 Harmony Full Parity        | NOT_STARTED         | 仅 codegen                                              |
| N4 iOS Full Parity            | NOT_STARTED         | 仅 codegen；Build 受 macOS 阻塞                         |
| N5 Cross-platform Conformance | PARTIAL_WITH_REPORT | Android PASS；Harmony/iOS 未跑                          |
| N6 Legacy Cutover             | NOT_STARTED         | Cutover 条件未满足                                      |
| N7 Native Production RC       | NOT_STARTED         | 等待三端原生闭环                                        |

## 17.2 Android 当前状态

已完成：

- 原生 Gradle 工程；
- `:core`；
- `:conformance`；
- 纯 Kotlin Domain；
- Impact / Plan / Scenario / State Machine / Schema / JSON；
- generated canonical enums；
- `.depmap` Crypto；
- JCS RFC8785；
- 64/64 conformance；
- derivedKey / ciphertext / tag 与 Legacy Reference 逐字节一致。

未完成：

- SQLCipher persistence；
- Schema v3 Android physical DB / migration；
- Repository implementation；
- Android Keystore；
- BiometricPrompt；
- App Lock；
- `:app` 模块；
- Jetpack Compose UI；
- APK/AAB；
- Emulator / Device E2E。

因此 Android 目前是：

> **“算法和跨端协议已经成立的 Kotlin Library”，还不是完整 Native App。**

## 17.3 Harmony 当前状态

当前仅有 codegen 产物。新的 Native `harmony/` 仍需建立真正的：

- ArkTS Domain；
- Conformance；
- ArkData；
- HUKS；
- ArkUI；
- hvigor Production build。

旧 `platforms/harmonyos/` 属于 UTS 时代产物，不得与新 Native 工程混为一谈。

## 17.4 iOS 当前状态

当前仅有 generated canonical code。仍需：

- Swift Domain；
- Impact / Plan / Drift / State Machine；
- `.depmap` Crypto；
- SQLCipher；
- Repository；
- SwiftUI；
- Conformance source；
- XCTest / Swift Testing。

在 Windows 上可以完成 Source Parity，但：

```text
IOS_BUILD = BLOCKED_BY_MACOS
```

必须保持真实口径。

---

# 18. 已发现并修正的历史问题

Native Migration 的价值之一是把 Legacy 中“之前测试没有覆盖的假设”暴露出来。

## 18.1 `bound_to` 语义冲突

Legacy UI 的可声明关系包含 `bound_to`，但 runtime registry 并不承认它。Native 版本不机械重演；Canonical Spec 以当前真实支持的 relation 为准，Future relation 留在 Backlog。

## 18.2 CSV Fixture 名不副实

曾存在：

- 名称声称含 BOM、实际没有 BOM；
- 名称声称缺 required column、实际并未真正缺列。

Native Conformance fixture 必须按内容而不是文件名验真。

## 18.3 wrong password Fixture Bug

迁移过程中曾把正确口令结果误当 wrong-password outcome；已经修正。说明 crypto fixture 自身也必须进行 Oracle selfcheck。

## 18.4 ICS / NaN → JSON null

拒绝 NaN 的案例如果被 JSON 序列化成 `null`，会破坏原测试语义。Fixture generator 必须在 serialization 前保持非法值测试意图。

## 18.5 Conformance 总状态命名

当前某些报告中的 Harness `VERDICT=PASS` 容易被误读成“三端跨平台已 PASS”。本母版冻结更严格口径：Harness 与 Cross-platform Verdict 分开。

---

# 19. Revised Roadmap：实际完成顺序

旧 v1.0 的 Roadmap 已被真实工程演进更新。本版正式采用下列编号：

## MVP01 — Payment Reality Kernel ✅

完成：

- 微信账单；
- Observation / Proposal / Reality boundary；
- Dependency / Group；
- payment Impact；
- `.depmap` V1；
- ChangePlan 初始闭环。

## MVP02 — Global Source Abstraction ✅

完成：

- EvidenceSourceAdapter；
- SourceInstance；
- Generic CSV；
- OFX/QFX；
- source-scoped fingerprint；
- Evidence provenance；
- RelationDefinitionRegistry；
- Schema v2；
- 多来源语义。

## Engineering Baseline v1 ✅

完成严格工程门禁、property/fuzz/mutation、安全、clean install/clone、architecture 与 agent 开发纪律。

## MVP03 — Living Graph & Change Safety ✅ / Freeze 完成后进入 Production Closure

完成：

- graphRevision；
- Rebase；
- PlanReadiness；
- ScenarioCoverage；
- RealityDrift；
- DiscoveryCandidate；
- ScenarioTemplate；
- Timeline；
- Verification；
- Schema v3。

## Native Migration N0–N7 ← 当前最高优先级

原生迁移完全优先于新增业务 Domain。

当前：

```text
N0 = PASS
N1 = PARTIAL
N2 = NEXT
N3/N4/N5/N6/N7 = 后续连续执行
```

## MVP04 — International Payment Infrastructure ⏸ 延后

只有 Native Production RC 稳定后再开始。

候选范围：

- `card_on_file`；
- `direct_debit_mandate`；
- `payout_destination`；
- `wallet_funding`；
- PayPal / overseas source；
- updater continuity hint；
- close bank account / wallet funding scenarios。

不要在 Native Migration 中提前实现。

## v1.x — Identity / Access / Recovery

换手机号、换邮箱、丢手机、2FA/passkey pointer、recovery dependency。

## v1.x — Digital Resource Infrastructure

Domain、Registrar、DNS、Hosting、GitHub、Repo、Cloud Resources、Provider Migration。

## Future — PersonalService / Continuity Goals

可以从“节点”进一步提升到用户 outcome：

- 主支付保持可用；
- 核心身份可恢复；
- 个人网站持续在线；
- 工作数字资产可访问。

## Future — Incident / Recovery Drill

丢手机、卡片被盗、邮箱被攻破等场景可以形成：

```text
Contain → Recover → Rotate → Verify
```

## Future — Digital Surface Discovery

Local-first discovery only；只生成 Candidate。

---

# 20. 发布、商店、真实数据与最终上线标准

## 20.1 Native Production RC 不等于 Store Submitted

必须区分：

```text
SOURCE_READY
BUILD_READY
INSTALL_READY
DEVICE_VERIFIED
SIGNING_READY
STORE_METADATA_READY
STORE_ASSETS_READY
STORE_SUBMISSION_READY
STORE_SUBMITTED
```

## 20.2 Android

最终要求：

- Gradle clean build；
- debug APK；
- release-compatible AAB；
- Keystore 配置；
- Emulator / Device smoke；
- Fresh Install E2E；
- Backup / Restore；
- Migration；
- permission audit；
- network/logging audit。

## 20.3 HarmonyOS

最终要求：

- DevEco / hvigor build；
- ArkData / HUKS 真验证；
- Device / Emulator smoke；
- signing / store metadata。

## 20.4 iOS

Windows 阶段做到 Source Parity；Mac 阶段完成：

- Xcode build；
- tests；
- Simulator；
- Physical device；
- Archive；
- Validate；
- TestFlight。

## 20.5 Real Data Gate

截至当前：

```text
REAL_DATA_CORRECTNESS = NOT_RUN
REAL_DATA_VALUE = NOT_RUN
```

公开推广 / Store 提交前，建议至少进行一次私有真实账单 Pilot，验证：

- Precision；
- false `must_change` = 0 或可解释；
- Node Resolution；
- Proposal utility；
- 是否真的发现用户容易遗漏的依赖；
- 是否缩短完整换卡排查时间。

Synthetic 不能冒充 Real Data Gate。

## 20.6 Store 文案边界

禁止声称：

- 100% 安全；
- 银行级安全（无独立依据时）；
- 自动管理银行账户；
- 官方银行合作；
- 能发现所有依赖；
- 已覆盖全部支付路径。

更准确表达：

> 基于用户导入、已确认和当前已知信息，帮助用户在数字基础设施发生变化前识别可能受影响的关系并形成检查计划。

---

# 21. Future Backlog

Native RC 前统一禁止进入：

- PayPal 正式 source；
- Open Banking / Plaid；
- card_on_file / mandate 等新 production relation；
- 手机号完整 access / recovery domain；
- 邮箱完整 recovery domain；
- Browser Discovery；
- OAuth / linked apps discovery；
- GitHub / Cloud / Domain 新 Domain；
- AI/LLM 直接参与 Reality 决策；
- Agent 产品能力；
- 云同步；
- 账号系统；
- 社交；
- 广告；
- 通用提醒。

AI 当前最适合的角色是**开发和多端同步助手**：根据 Canonical Spec、Fixture 与一端已验证实现，帮助生成 / 比较 Kotlin、Swift、ArkTS；但 AI 不能绕过 Conformance，也不能直接成为产品 Reality 决策源。

---

# 22. 设计决策冻结表

- **产品类别**：Personal Digital Infrastructure + Personal Digital Change Management。
- **产品名 / 模型**：个人数字基础设施图谱 / PDIG；工程代号 DepMap。
- **产品核心**：Evidence → Human Confirmation → Confirmed Reality → Impact → ChangePlan → Verification。
- **工程第一原则**：宁可漏报，不可把不确定伪装成必须处理。
- **Graph**：State model，不是首页。
- **用户入口**：Answer-oriented / Scenario-oriented。
- **MVP Domain**：payment。
- **Active Scenario**：replace / expiring / close payment instrument。
- **Reality Boundary**：Observation / Proposal / Candidate / Drift 全部不等于 Reality。
- **Group**：必须确认；两条边不等于 fallback。
- **Criticality**：required / unknown；机器不自动 required。
- **Impact Key**：`(nodeId, capability)`。
- **Readiness**：blocked / review_required / ready_with_known_scope。
- **Coverage**：unknown / limited / partial / well_evidenced；不是安全分。
- **Drift**：positive evidence only；不自动改 Reality。
- **Verification**：done != verified。
- **Timeline**：Projection，不是 Truth。
- **Source**：Global-first / Source-by-source。
- **当前 Sources**：WeChat / Generic CSV / OFX-QFX / Manual 基础。
- **Schema**：Logical v3。
- **`.depmap`**：DEPMAP_CONTAINER_V1 冻结。
- **Privacy**：local-first；raw statements 不长期保存。
- **Android**：Kotlin + Compose + SQLCipher + Keystore + BiometricPrompt。
- **iOS**：Swift + SwiftUI + SQLCipher + Keychain + LocalAuthentication。
- **Harmony**：ArkTS + ArkUI + ArkData + HUKS。
- **Production 跨端策略**：Canonical Spec + Codegen + Shared Fixtures + Conformance。
- **Legacy**：Behavior Oracle / Reference，不再是未来 Production Runtime。
- **DCloud / UTS / uni-app**：Native Cutover 后 Production dependency = 0。
- **Native 当前阶段**：N0 PASS；Android N1/N2 进行中。
- **MVP04**：Native Production RC 后再开始 International Payment Infrastructure。
- **AI 产品决策**：不进入 Reality 决策链。
- **AI 工程角色**：可辅助三端实现与差异审计，但必须服从 Spec / Fixture / Gate。

---

# 附录 A：端到端数据流

```text
                       Evidence Sources
    ┌───────────────────────────────────────────────┐
    │ WeChat / Generic CSV / OFX-QFX / Manual      │
    │ Future: platform export / open banking /      │
    │          discovery candidate source           │
    └──────────────────────┬────────────────────────┘
                           ↓
                   EvidenceSourceAdapter
                           ↓
                    SourceInstance
                           ↓
                  Observation (memory)
                           ↓
       Normalization / Fingerprint / CanonicalEvent
                           ↓
                    Node Resolution
                    ↙               ↘
        DiscoveryCandidate      DependencyProposal
                    ↓               ↓
                 User Confirmation
                    ↓               ↓
                  Node        Dependency / Group
                    └───────┬───────┘
                            ↓
                   Confirmed Reality Graph
                            ↓
                 graphRevision / Freshness
                            ↓
                    simulateScenario()
                            ↓
                       ImpactResult
                            ↓
                  Scenario / ChangePlan
                            ↓
                         Actions
                            ↓
                      Verification
                            ↓
              Drift / Rebase / Timeline / History
```

---

# 附录 B：三端原生迁移与 Conformance

```text
Legacy TypeScript / uni-app x
        │
        ├─ Freeze tag
        └─ Behavior Oracle
               │
               ↓
        Canonical Spec (machine-readable)
               │
      ┌────────┴─────────┐
      │                  │
    Codegen          Golden Fixtures
      │                  │
      ├──── Kotlin ──────┤
      ├──── Swift ───────┤
      └──── ArkTS ───────┤
                         ↓
               Conformance Harness
                         ↓
              normalized semantic diff
                         ↓
        ┌────────────────┼────────────────┐
        ↓                ↓                ↓
 Android Native       iOS Native      Harmony Native
 Kotlin/Compose      Swift/SwiftUI     ArkTS/ArkUI
 SQLCipher           SQLCipher         ArkData
 Keystore            Keychain          HUKS
        └────────────────┼────────────────┘
                         ↓
                  Native Cutover
                         ↓
     DCloud / UTS / uni-app Production dependency = 0
```

---

# 附录 C：当前 Gate 状态

```text
LEGACY_ORACLE                  PASS
CANONICAL_SPEC                 PASS
CODEGEN                        PASS
FIXTURE_INTEGRITY              PASS
CONFORMANCE_HARNESS            PASS
ANDROID_DOMAIN                 PASS
ANDROID_CRYPTO                 PASS
ANDROID_JCS                    PASS
ANDROID_CONFORMANCE            PASS (64/64)
ANDROID_PERSISTENCE            NOT_STARTED
ANDROID_SECURITY               NOT_STARTED
ANDROID_COMPOSE_UI             NOT_STARTED
ANDROID_APK_AAB                NOT_STARTED
ANDROID_RUNTIME                NOT_RUN
HARMONY_DOMAIN                 NOT_STARTED
HARMONY_CONFORMANCE            NOT_RUN
IOS_DOMAIN                     NOT_STARTED
IOS_CONFORMANCE                NOT_RUN
IOS_BUILD                      BLOCKED_BY_MACOS
CROSS_PLATFORM_CONFORMANCE     PARTIAL_WITH_REPORT
LEGACY_CUTOVER                 NOT_STARTED
NATIVE_PRODUCTION_RC           NOT_STARTED
REAL_DATA_CORRECTNESS          NOT_RUN
REAL_DATA_VALUE                NOT_RUN
STORE_SUBMITTED                NO
```

---

# 附录 D：术语对照

| 内部术语               | 用户界面建议               |
| ---------------------- | -------------------------- |
| RealityDrift           | 可能发生了变化             |
| ScenarioCoverage       | 本次分析依据               |
| DiscoveryCandidate     | 待确认服务                 |
| Verification           | 验证                       |
| needs_revalidation     | 需要重新检查               |
| blocked                | 还有必须处理的事项         |
| review_required        | 还有信息需要确认           |
| ready_with_known_scope | 基于当前信息，可以继续     |
| DependencyProposal     | 待确认关系 / 建议确认      |
| SourceInstance         | 数据来源                   |
| GraphRevision          | 不直接显示；高级诊断中可见 |

---

# 附录 E：新对话接力模板

以下文本可以直接作为下一次新对话的开头：

```text
我们继续做 PDIG / DepMap（个人数字基础设施图谱）。请先沿用当前最新母版《个人数字基础设施图谱_完整设计与工程母版_v2.0-R1_三端原生迁移整合版_2026-09-15》，不要回到旧版 uni-app x 路线，也不要重新设计 MVP01–MVP03。

当前已经完成：
1. MVP01 Payment Reality Kernel；
2. MVP02 Global Source Abstraction（SourceInstance / Generic CSV / OFX-QFX / Relation Registry / Schema v2）；
3. Engineering Baseline；
4. MVP03 Living Graph & Change Safety（graphRevision / Rebase / PlanReadiness / ScenarioCoverage / RealityDrift / DiscoveryCandidate / ScenarioTemplate / Timeline / Verification / Schema v3）；
5. Legacy uni-app x 已冻结为 Behavior Oracle，tag 为 v0.3.0-uniapp-reference；
6. Native Migration N0-A～N0-F 已 PASS：Canonical Spec、三端 Codegen、64 个平台中立 fixture、Conformance Harness 已建立；
7. Android 原生 Domain / Crypto / JCS / Conformance 已 PASS，当前 Android Conformance 64/64；`.depmap` derivedKey/ciphertext/tag 与 Legacy Oracle 逐字节一致。

当前没有完成：
- Android SQLCipher persistence / migration / Repository；
- Android Keystore / Biometric / App Lock；
- Android Compose App / APK / Emulator E2E；
- Harmony Native Domain / ArkData / HUKS / ArkUI / Conformance；
- iOS Native Domain / SQLCipher / Keychain / SwiftUI / Conformance；iOS build 受当前无 macOS/Xcode 阻塞；
- Cross-platform Conformance 目前只能算 PARTIAL_WITH_REPORT，而不是三端 PASS；
- Legacy Cutover 与 Native Production RC 尚未进行；
- Real Data Gate 仍是 NOT_RUN。

当前最高优先级：继续 Native Migration，不进入 MVP04。先完成 Android N1/N2：data（SQLCipher + Schema v3 migration + Repository）→ security（Keystore + Biometric）→ app（Jetpack Compose Vertical Slice → Full Parity）→ assemble APK → Emulator E2E；然后连续进入 Harmony N3、iOS N4、N5 Conformance、N6 Cutover、N7 Native RC。

永久原则：宁可漏报，不把不确定伪装成必须处理；Observation / Proposal / Candidate / Drift 都不等于 Reality；Group 必须确认；done != verified；Coverage != Readiness；Graph 是 state model，不是首页。Production 最终目标是 Android=Native、Harmony=Native、iOS=Native，DCloud/UTS/uni-app Production dependency=0。

请基于这些状态继续，不要让我重新解释历史。
```

---

## 本版结论

PDIG 已经从“一个想法 + 关系图”演进到具有严格 Reality Boundary、Impact、Living Graph、ChangePlan、local-first security 与跨端协议的完整产品体系。当前最大的工作不再是继续发明业务能力，而是把已经被 Legacy Oracle 和 Android Kotlin Conformance 证明过的 Canonical Semantics，完整落到 Android / HarmonyOS / iOS 三端原生产品中。

因此当前唯一正确的工程主线是：

> **Android N1/N2 → Harmony N3 → iOS N4 → Cross-platform Conformance N5 → Legacy Cutover N6 → Native Production RC N7 → Real Data Pilot / Store Submission → 再进入 MVP04。**
