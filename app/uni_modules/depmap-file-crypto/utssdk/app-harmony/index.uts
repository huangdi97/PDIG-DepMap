/** HarmonyOS 实现：cryptoFramework AES-GCM + Argon2id 纯 ArkTS 移植。IMPLEMENTED(partial)；COMPILED=NO。 */
export function encryptToDepmap(plaintext: Uint8Array, password: string): Promise<string> {
  return new Promise((resolve, reject) => { reject(new Error('Harmony argon2id port pending golden vector integration')) })
}
export function decryptDepmap(json: string, password: string): Promise<Uint8Array> {
  return new Promise((resolve, reject) => { reject(new Error('Harmony argon2id port pending golden vector integration')) })
}
