<script lang="uts">
import { checkUnlocked } from './stores/app-state'

export default {
  onLaunch: function () {
    // 首页显示最近一次用户主动导出备份时间（CANONICAL §8.10）
    console.log('DepMap launched')
  },
  onShow: function () {
    // 切后台隐私策略由 PrivacyScreenAdapter 处理
  },
  onHide: function () {
    // 后台遮罩（iOS）/ FLAG_SECURE（Android）已由插件接管
  }
}
</script>

<style>
/* 全局基础样式 */
page {
  background-color: #f7f8fa;
  font-size: 15px;
  color: #1f2329;
}
.card {
  background-color: #ffffff;
  border-radius: 12px;
  padding: 16px;
  margin: 12px;
}
.card-title {
  font-size: 16px;
  font-weight: bold;
  margin-bottom: 8px;
}
.hint {
  color: #86909c;
  font-size: 13px;
}
.btn-primary {
  background-color: #165dff;
  color: #ffffff;
  border-radius: 10px;
  padding: 12px 0;
  text-align: center;
  font-size: 16px;
}
.btn-danger {
  background-color: #f53f3f;
  color: #ffffff;
  border-radius: 10px;
  padding: 12px 0;
  text-align: center;
  font-size: 16px;
}
.level-must {
  color: #f53f3f;
  font-weight: bold;
}
.level-review {
  color: #ff7d00;
}
.level-ok {
  color: #00b42a;
}
</style>
