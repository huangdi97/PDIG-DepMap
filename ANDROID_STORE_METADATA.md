# ANDROID_STORE_METADATA（Draft）

> 状态：**DRAFT / NOT_SUBMITTED**
> 这是**商店上架材料草稿**，不是已上架内容，也不是安全承诺。
> 本文件遵循项目一贯原则：**宁可漏报，不可把不确定伪装成承诺**。

更新时间：2026-09-15
适用范围：Android（PDIG / DepMap 原生端）

---

## 1. 应用名称

- 主名称：**PDIG**
- 副标题 / short name：**个人数字依赖图**
- 包名：`com.pdig.app`

---

## 2. Short description（≤80 字符）

> 本地梳理账号、卡片与订阅之间的依赖关系，换卡或停用时先看会影响什么。

（长度 38 字符，符合 Play 商店 80 字符限制）

---

## 3. Full description

PDIG 是一个**本地优先（local-first）**的个人数字基础设施梳理工具。

它帮你回答一个具体问题：

> "如果我要换掉这张卡 / 停用这个账号 / 到期不续这个订阅，会牵动哪些东西？"

### 它做什么

- 把你在意的数字服务整理成一张**依赖关系图**：账号、支付方式、订阅、邮箱、手机号之间的连接。
- 当你计划一次变更（例如更换支付卡），它会列出**受影响的下游对象**，并给出一份可执行的**变更清单**。
- 变更完成后，可以逐项**标记完成并核实**，避免遗漏。

### 它不做什么

- **不连接你的银行账户**，不读取交易流水，不做自动扣款或代付。
- **不自动发现**你所有的依赖关系 —— 依赖主要由你明确录入与确认，导入功能只作为辅助。
- **不上传**你的数据。全部数据只保存在本机。

### 本地优先

- 数据存储在设备本地数据库（SQLCipher 加密）。
- 应用**不申请网络权限**（Android 清单中不含 `INTERNET`），因此无法把数据发送到任何服务器。
- 备份 / 恢复通过你主动导出的加密文件（`.depmap`）完成，文件只存在你指定的位置。

---

## 4. 隐私摘要（Privacy summary）

| 项目                        | 说明                                                       |
| --------------------------- | ---------------------------------------------------------- |
| 数据存储位置                | 仅设备本地                                                 |
| 是否上传服务器              | 否（无网络权限）                                           |
| 是否读取银行/交易数据       | 否                                                         |
| 是否包含第三方统计/广告 SDK | 否                                                         |
| 是否需要注册账号            | 否                                                         |
| 导入的账单文件如何处理      | 仅在本机解析，用于生成待确认的依赖建议；原始文件不会被上传 |
| 备份文件                    | 由用户主动导出，AES-256-GCM 加密，口令由用户设置           |

---

## 5. 权限说明（Permissions explanation）

Android 清单实际申请的权限（以构建产物 `app-debug.apk` 的 `AndroidManifest.xml` 为准）：

| 权限                                       | 用途                                   | 是否必需                   |
| ------------------------------------------ | -------------------------------------- | -------------------------- |
| `USE_BIOMETRIC` / `USE_FINGERPRINT`        | 应用锁的生物识别解锁                   | 可选，仅当启用应用锁时使用 |
| `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` | 由 AndroidX 自动注入，非本应用主动申请 | 系统自动                   |

**未申请**的权限：`INTERNET`、`ACCESS_NETWORK_STATE`、`READ_EXTERNAL_STORAGE`、
`WRITE_EXTERNAL_STORAGE`、`CAMERA`、`LOCATION` 系列、`READ_CONTACTS`、`READ_SMS`。

> 备份 / 恢复使用系统文件选择器（Storage Access Framework），由用户显式授权具体文件，
> 不申请全局存储权限。

---

## 6. 支持的场景（Supported scenarios）

当前内置 3 个**支付类**场景模板（MVP 范围，与 `ScenarioRegistry.active` 一致）：

1. **更换支付卡（replace_payment_card）** —— 换卡前检查支付钱包、自动扣款和订阅关系。
2. **银行卡即将到期（expiring_payment_card）** —— 到期前检查仍依赖这张卡的支付路径（建议提前 30 天）。
3. **注销银行卡（close_payment_instrument）** —— 注销前确认哪些支付关系需要迁移（建议提前 14 天）。

产品按「需要你处理 / 可能发生了变化 / 即将到来 / 常用场景 / 我的基础设施」组织首页，
场景执行完整走过：影响面 → 变更计划 → 逐项完成 → 逐项验证（done ≠ verified）。
场景覆盖度按 `unknown / limited / partial / well_evidenced` 分级，
**"unknown" 会如实显示，不会伪装成已覆盖。**

> 说明：早期文档曾把场景误写为「更换/停用邮箱」与「停用订阅」，与 `ScenarioRegistry.active`
> （3 个支付场景）不一致，已按 canonical 修正。

---

## 7. 数据处理说明（Data handling）

- 本地数据库：SQLCipher 加密，数据库口令由 Android Keystore 包装后存储（不明文落盘）。
- 导入解析：账单文件在本机解析；解析出的 `Observation` 只存在于导入会话内存中，**不落库**；
  只有经过用户确认的依赖关系才会写入图。
- 日志：release 构建不输出 SQL、堆栈或口令，错误信息以错误码形式记录。
- 卸载：卸载应用会一并删除本地数据库与密钥材料，**无法找回**，因此建议自行导出 `.depmap` 备份。

---

## 8. 截图清单（Screenshots checklist）

需要准备（尚未制作）：

- [ ] 首页（回答式首页）
- [ ] 场景中心
- [ ] 基础设施总览
- [ ] 依赖图（二级页面）
- [ ] 变更计划详情
- [ ] 时间线
- [ ] 待确认项
- [ ] 导入 / 导入映射
- [ ] 备份 / 恢复

> 截图不得包含真实账单、真实卡号、真实手机号等个人数据。

---

## 9. 图标与素材清单（Icon / feature graphic checklist）

- [ ] 应用图标（512×512，自适应图标含前景/背景层）
- [ ] Feature graphic（1024×500）
- [ ] 应用图标目前使用 placeholder（Android 默认），**上架前必须替换**

---

## 10. 隐私政策（Privacy policy requirement）

**必须在上架前提供可公开访问的隐私政策链接。**

需包含：

- 数据仅本地存储、不上传
- 不收集个人信息
- 不包含第三方统计/广告 SDK
- 数据删除方式（卸载应用 / 清除数据）
- 联系方式

> 本文件不是隐私政策本身，仅为需求清单。

---

## 11. Release notes 模板

```
版本 0.1.0-milestone

- 本地依赖关系图：账号 / 支付方式 / 订阅 的连接梳理
- 变更影响预览：换卡、换邮箱、停用订阅前先看影响面
- 变更计划：生成可勾选的变更清单，逐项完成并核实
- 本地加密存储：SQLCipher + Android Keystore
- 加密备份 / 恢复：.depmap 文件（AES-256-GCM）
- 账单导入辅助：微信账单 / 通用 CSV / OFX-QFX（仅本机解析）

已知限制：
- 依赖主要依靠用户明确录入与确认，不做自动发现
- 不包含网络同步功能
- 未接入银行或任何金融服务机构
```

---

## 12. 禁止出现的表述（合规红线）

以下表述在本项目的任何商店材料中**均不得出现**：

- "100% 安全" / "绝对安全"
- "银行级安全"（bank-grade security）
- "发现所有依赖" / "自动找出你所有的账号"
- "与银行官方合作" / 任何暗示官方金融机构背书的表述
- "自动管理银行账户" / "自动扣款" / "自动续费管理"

---

## 13. 当前状态

| 项                     | 状态                              |
| ---------------------- | --------------------------------- |
| 文案草稿               | 完成                              |
| 截图                   | NOT_STARTED                       |
| 图标 / feature graphic | NOT_STARTED（当前为 placeholder） |
| 隐私政策链接           | NOT_STARTED（必需，阻塞上架）     |
| 实际提交商店           | NOT_STARTED                       |
